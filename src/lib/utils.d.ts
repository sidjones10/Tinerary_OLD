declare module "@/lib/utils" {
    // Declare the types for the functions or variables in utils.js
    export function someUtilityFunction(arg: string): number;
    export function anotherUtilityFunction(arg: number): string;
    // Add more function signatures or type declarations as needed
  }
  